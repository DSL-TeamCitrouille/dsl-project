game DamDamFastDark {
  board { size 8 }

  pieces {
    piece whiteShadow { color silver quantity 12 }
    piece blackShadow { color silver quantity 12 }
  }

  rules {
    movement {
      alternating whiteShadow and blackShadow
      direction diagonal
      canMoveBackward true
    }
    capture {
      mandatory true
      message "Une capture illumine brièvement le plateau."
    }
    action {
      type both
    }
  }

  objective {
    winByForfeit { allowed true }
    winByCapture { captureAll opponent }
  }

  ui {
    theme {
      lightSquares: "darkgray"
      darkSquares: "black"
      highlight: "crimson"
    }
    layout { showCaptured false }
  }

  settings {
    animationSpeed: 5
    showLegalMoves: false
    aiDifficulty: 3
  }
}
